
name: Flutter CI/CD with Telegram

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17.0.10'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.1'
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Get version info
        id: version
        run: |
          echo "VERSION=$(grep 'version:' pubspec.yaml | cut -d ' ' -f 2)" >> $GITHUB_OUTPUT
          echo "COMMIT_MSG=$(git log -1 --pretty=%B)" >> $GITHUB_OUTPUT

      - name: Send APK to Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          curl -F chat_id="${TELEGRAM_CHAT_ID}" \
               -F document=@"build/app/outputs/flutter-apk/app-release.apk" \
               -F caption="🚀🚀🚀🚀 New Build Available " \
               https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument

      - name: Send notification
        if: always()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            STATUS="✅ Success"
          else
            STATUS="❌ Failed"
          fi
          
          curl -s -X POST https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage \
               -d chat_id="${TELEGRAM_CHAT_ID}" \


  build-web:
    name: Build Web App
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.1'
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get

      - name: Build Web
        run: flutter build web --release

      - name: Get version info
        id: version
        run: |
          echo "VERSION=$(grep 'version:' pubspec.yaml | cut -d ' ' -f 2)" >> $GITHUB_OUTPUT
          echo "COMMIT_MSG=$(git log -1 --pretty=%B)" >> $GITHUB_OUTPUT

      - name: Archive Web Build
        run: |
          cd build/web
          zip -r ../../web-build.zip .
          cd ../..
          ls -lh web-build.zip

      - name: Send Web Build to Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ -z "$TELEGRAM_BOT_TOKEN" ] || [ -z "$TELEGRAM_CHAT_ID" ]; then
            echo "⚠️ Telegram secrets not set. Skipping notification."
            exit 0
          fi
          
          echo "✅ Sending Web build to Telegram..."
          
          RESPONSE=$(curl -F chat_id="${TELEGRAM_CHAT_ID}" \
               -F document=@"web-build.zip" \
               -F caption="🌐🌐🌐 Web Build" \
               https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument)
          
          if echo "$RESPONSE" | grep -q '"ok":true'; then
            echo "✅ Successfully sent Web build to Telegram!"
          else
            echo "❌ Failed to send to Telegram: $RESPONSE"
            exit 1
          fi
      - name: Send notification
        if: always()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            STATUS="✅ Success"
          else
            STATUS="❌ Failed"
          fi
          
          curl -s -X POST https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage \
               -d chat_id="${TELEGRAM_CHAT_ID}" \


#
#  build-windows:
#    name: Build Windows App
#    runs-on: windows-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      - name: Setup Flutter
#        uses: subosito/flutter-action@v2
#        with:
#          flutter-version: '3.24.0'
#          channel: 'stable'
#
#      - name: Install dependencies
#        run: flutter pub get
#
#      - name: Build Windows
#        run: flutter build windows --release
#
#      - name: Get version info
#        id: version
#        shell: bash
#        run: |
#          echo "VERSION=$(grep 'version:' pubspec.yaml | cut -d ' ' -f 2)" >> $GITHUB_OUTPUT
#          echo "COMMIT_MSG=$(git log -1 --pretty=%B)" >> $GITHUB_OUTPUT
#
#      - name: Remove unnecessary files
#        shell: powershell
#        run: |
#          Remove-Item -Recurse -Force build/windows/x64/runner/Release/*.pdb -ErrorAction SilentlyContinue
#          Remove-Item -Recurse -Force build/windows/x64/runner/Release/*.exp -ErrorAction SilentlyContinue
#          Remove-Item -Recurse -Force build/windows/x64/runner/Release/*.lib -ErrorAction SilentlyContinue
#
#      - name: Archive Windows Build
#        shell: powershell
#        run: |
#          Compress-Archive -Path build/windows/x64/runner/Release/* -DestinationPath windows-build.zip
#          Get-Item windows-build.zip | Select-Object Name, Length
#
#      - name: Send Windows Build to Telegram
#        shell: bash
#        env:
#          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
#          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
#        run: |
#          if [ -z "$TELEGRAM_BOT_TOKEN" ] || [ -z "$TELEGRAM_CHAT_ID" ]; then
#            echo "⚠️ Telegram secrets not set. Skipping notification."
#            exit 0
#          fi
#
#          echo "✅ Sending Windows build to Telegram..."
#
#          RESPONSE=$(curl -F chat_id="${TELEGRAM_CHAT_ID}" \
#               -F document=@"windows-build.zip" \
#               -F caption="🪟 Windows Build%0AVersion: ${{ steps.version.outputs.VERSION }}%0ACommit: ${{ steps.version.outputs.COMMIT_MSG }}%0ABranch: ${GITHUB_REF##*/}%0AArchitecture: x64" \
#               https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument)
#
#          if echo "$RESPONSE" | grep -q '"ok":true'; then
#            echo "✅ Successfully sent Windows build to Telegram!"
#          else
#            echo "❌ Failed to send to Telegram: $RESPONSE"
#            exit 1
#          fi

#  notify-completion:
#    name: Send Summary
#    runs-on: ubuntu-latest
#    needs: [ build-android, build-web, build-windows ]
#    if: always()
#
#    steps:
#      - name: Send completion notification
#        env:
#          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
#          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
#        run: |
#          if [ -z "$TELEGRAM_BOT_TOKEN" ] || [ -z "$TELEGRAM_CHAT_ID" ]; then
#            echo "⚠️ Telegram secrets not set. Skipping notification."
#            exit 0
#          fi
#
#          # Determine overall status
#          if [ "${{ needs.build-android.result }}" == "success" ] && \
#             [ "${{ needs.build-web.result }}" == "success" ] && \
#             [ "${{ needs.build-windows.result }}" == "success" ]; then
#            STATUS="✅ All Builds Successful!"
#            EMOJI="🎉"
#          else
#            STATUS="⚠️ Build Summary"
#            EMOJI="📊"
#          fi
#
#          # Build status emojis
#          ANDROID_STATUS="${{ needs.build-android.result }}"
#          WEB_STATUS="${{ needs.build-web.result }}"
#          WINDOWS_STATUS="${{ needs.build-windows.result }}"
#
#          case "$ANDROID_STATUS" in
#            "success") ANDROID_EMOJI="✅" ;;
#            "failure") ANDROID_EMOJI="❌" ;;
#            *) ANDROID_EMOJI="⚠️" ;;
#          esac
#
#          case "$WEB_STATUS" in
#            "success") WEB_EMOJI="✅" ;;
#            "failure") WEB_EMOJI="❌" ;;
#            *) WEB_EMOJI="⚠️" ;;
#          esac
#
#          case "$WINDOWS_STATUS" in
#            "success") WINDOWS_EMOJI="✅" ;;
#            "failure") WINDOWS_EMOJI="❌" ;;
#            *) WINDOWS_EMOJI="⚠️" ;;
#          esac
#
#          # Build message
#          MESSAGE="${EMOJI} ${STATUS}%0A%0A"
#          MESSAGE="${MESSAGE}📱 Android: ${ANDROID_EMOJI} ${ANDROID_STATUS}%0A"
#          MESSAGE="${MESSAGE}🌐 Web: ${WEB_EMOJI} ${WEB_STATUS}%0A"
#          MESSAGE="${MESSAGE}🪟 Windows: ${WINDOWS_EMOJI} ${WINDOWS_STATUS}%0A%0A"
#          MESSAGE="${MESSAGE}📦 Repository: ${{ github.repository }}%0A"
#          MESSAGE="${MESSAGE}🌿 Branch: ${GITHUB_REF##*/}%0A"
#          MESSAGE="${MESSAGE}👤 Author: ${{ github.actor }}%0A"
#          MESSAGE="${MESSAGE}🔗 Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
#
#          curl -s -X POST https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage \
#               -d chat_id="${TELEGRAM_CHAT_ID}" \
#               -d parse_mode="HTML" \
#               -d text="${MESSAGE}"
#
#          echo "✅ Summary notification sent!"
